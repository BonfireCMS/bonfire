{{action-bar for="pages:edit" newPage="newPage" toggleSideBar="toggleSideBar"}}
<div class="page__content">
  {{bf-editor-bar
  showContent=showContent
  showPreview=showPreview
  showSettings=showSettings
  toggleEditorState=(action "toggleEditorState")
  }}

  {{#if showContent}}
  <div class="bf-editor-box">
    <form class="form bf-editor__form">
      <div class="form__group">
        <label class="form__label">Title</label>
        {{input class="form__input bf-editor__input"value=page.name}}
      </div>
      {{bf-editor value=page.markdown}}
    </form>
  </div>
  {{/if}}

  {{#if showPreview}}
  <div class="bf-preview-box">
    {{bf-editor-preview markdown=page.markdown}}
  </div>
  {{/if}}

  {{#if showSettings}}
  <!-- settings -->
  {{/if}}
</div>
