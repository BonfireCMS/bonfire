---

postgres:
  container_name: bonfire_test_postgres
  extends:
    file: common.yml
    service: postgres
  volumes:
    - ../../../content/data/postgres-prod:/var/lib/postgresql/data

redis:
  container_name: bonfire_test_redis
  extends:
    file: common.yml
    service: redis

server:
  container_name: bonfire_test_server
  extends:
    file: common.yml
    service: server
  command: npm test
  links:
    - postgres
    - redis
  ports:
    - "3800:3800"

